/*
 * Gameboard.
 * Represents the state of the tic tac toe board and its 9 squares..
 * An array is used with the 9 squares as getValue.
 * getBoard()
 * markSquare()
 * printBoard() for console version
 */

/*
 * Square.
 * Square object represents a single square on the board.
 * markSquare(player)
 * getValue()
 *
 * Each square can have these values:
 * 0 = No marking
 * 1 = X (for player 1s marking)
 * 2 = O (for player 2s marking)
 */

/*
 * GameController.
 * Controls the flow and state of players turns and checks for wins and ties.
 * Array with both player objects containing name and mark(1=X 2=O)
 * switchPlayerTurn()
 * printNewRound() reprint board with changes.
 */
